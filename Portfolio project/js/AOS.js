document.addEventListener('DOMContentLoaded', function () {
    // Initialiseer AOS-bibliotheek
    AOS.init({
      duration: 1000, // Duur van de animatie in milliseconden
      mirror: true,   // Reflecteer de animatie voor een betere zichtbaarheid
    });
  });